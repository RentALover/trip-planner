spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trip_planner?useSSL=true&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.tripplanner: INFO
